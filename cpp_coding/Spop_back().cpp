// s.pop_back() is used to erase the last character of string reduce by one
#include <iostream>
using namespace std;
int main()
{
    string s = "Programming";
    // it will remove last string character
    s.pop_back();
    cout << s;
    return 0;
}