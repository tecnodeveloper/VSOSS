//Bitwise operator AND(&), OR(|), NOT(~) ,xor(^)
//Bitwise operator does't work on float its work only on int and char
#include<iostream>
using namespace std;
int main()
{
	int x = 11, y = 7,z;
	z = x&y;
	cout<<"Bitwise AND is: "<<z;
	return 0;
}

