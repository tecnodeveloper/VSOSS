"use strict";
/* 
// Void functions are those who return no value just they output values
function PrintName() {
  console.log(`My name is Zain Ali`);
} */
/*
PrintName();
PrintName();
PrintName();

 function FruitMixer(apples, bannana, milk) {
  const Juice = `Juice is ready with ${apples} apples and ${bannana} bannana and ${milk} milk packets`;
  return Juice;
  const functionReturn = FruitMixer(2, 1, 1);

  } 

console.log(
  `You can use the function return value in different lines of code ${functionReturn}`
);

// Function expression
const fruitMixer = function (apple, banana) {
  console.log(
    `You have function expression ${apple} apple and ${banana} bannana`
  );
};

console.log(fruitMixer(2, 3));

// Arrow function

const sum = (a, b) => a + b;

console.log(sum(2, 8));

function cutFruit(fruits) {
  return fruits * 3;
}

function FruitMixer(apples, bannana, milk) {
  const cutPiecesApple = cutFruit(apples);
  const cutPiecesBannana = cutFruit(bannana);
  const Juice = `Juice is ready with ${cutPiecesApple} apples and ${cutPiecesBannana} bannana and ${milk} milk packets`;
  return Juice;
}
const functionReturn = FruitMixer(2, 1, 1);
console.log(functionReturn);
q*/

/* console.log(`Original Array`);

const friends = ["Ammar", "khizar", "khuram", "Mustfa"];
console.log(friends);
friends.push("hafiz");
console.log(`After using push in array`);

console.log(friends);
console.log(`After using pop in array`);

friends.pop();

console.log(friends);
console.log(`After using unshift in array`);
friends.unshift("nobody");
console.log(friends);
console.log(`After using shift in array`);
friends.shift();
console.log(friends);
 */
/* 
const zain = {
  firstname: "Zain",
  lastName: "Ali",
  job: "teacher",
  isDriverLicence: true,
  birthyear: 2004,
  profession: "coding",
  friends: ["Micheal", "Thomas", "hamza"],
  // Function expression
  calage: function () {
    return 2024 - this.birthyear;
  },
}; */

// console.log(zainArray);

// Coding challenges
/* 
  zain has 3 friends and his best friend is called Thomas
*/

/* zainArray.location = "Gujranwala";
console.log(zainArray.location);

console.log(
  `${zainArray.firstname} has ${Number(
    zainArray.friends.length
  )} friends and his best friend is called ${zainArray.friends[1]}`
);
 */

// console.log(zain.calage());

// Challenge
// zain is 20-year old student and he has driver licence.

/* console.log(
  `${zain.firstname} is ${zain.calage()}-year old student and he has ${
    zain.isDriverLicence ? "a" : " no"
  }  driving licence`
);
 */

// We use loop to remove the repition of code.....
